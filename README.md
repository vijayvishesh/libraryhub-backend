# library-service

Backend service scaffolded from the shared backend service structure.

## Local setup


1. Install dependencies with `yarn install`.
2. Start the service with `yarn dev`.

## Verification

- Lint: `yarn lint`
- Build: `yarn build`
- Test: `yarn test`
